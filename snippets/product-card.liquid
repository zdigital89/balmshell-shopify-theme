{%- assign fallback_image = fallback | default: 'oat-coconut.png' -%}
{%- assign is_coming_soon = false -%}
{%- assign is_sold_out = false -%}
{%- assign display_title = product.title | replace: '—', ' ' | replace: '–', ' ' | replace: ' - ', ' ' | replace: ' | ', ' ' -%}
{%- case product.handle -%}
  {%- when 'glow-getter-skin-cinnamon-copy' -%}
    {%- assign display_title = 'Glow-Getter Skin Soap' -%}
  {%- when 'soft-bloom-skin-rosy-glow-soap-bar-copy' -%}
    {%- assign display_title = 'Soft Bloom Skin Soap Bar' -%}
  {%- when 'petal-me-softly-soap-part-of-our-calendula-collection' -%}
    {%- assign display_title = 'Petal Me Softly Soap' -%}
  {%- when 'kiss-my-balm-shimmer-bronzer-lotion-bar' -%}
    {%- assign display_title = 'Kiss My Balm Lotion Bar' -%}
  {%- when 'calendula-whipped-body-butter' -%}
    {%- assign display_title = 'Calendula Whipped Body Butter' -%}
  {%- when 'sud-buddies-hanging-hand-soap' -%}
    {%- assign display_title = 'Sud Buddies Hanging Hand Soap' -%}
  {%- when 'sud-buds-hanging-hand-soap' -%}
    {%- assign display_title = 'Sud Buddies Hanging Hand Soap' -%}
  {%- when 'sud-buddies-hanging-hand-soap-for-kids' -%}
    {%- assign display_title = 'Sud Buddies Hanging Hand Soap' -%}
  {%- when 'lavender-coconut-triple-butter-soap' -%}
    {%- assign display_title = 'Lavender Coconut Triple Butter Soap' -%}
    {%- assign is_sold_out = true -%}
  {%- when 'august-challenge-lavender-coconut-triple-butter-soap' -%}
    {%- assign display_title = 'Lavender Coconut Triple Butter Soap' -%}
    {%- assign is_sold_out = true -%}
  {%- when 'lavender-coconut-soap-limited-edition' -%}
    {%- assign display_title = 'Lavender Coconut Triple Butter Soap' -%}
    {%- assign is_sold_out = true -%}
{%- endcase -%}
{%- if product.tags contains 'coming-soon' -%}{%- assign is_coming_soon = true -%}{%- endif -%}
{%- if product.tags contains 'sold-out' -%}{%- assign is_sold_out = true -%}{%- endif -%}
{%- if product.available == false and is_coming_soon == false -%}{%- assign is_sold_out = true -%}{%- endif -%}
{%- if is_sold_out -%}{%- assign is_coming_soon = false -%}{%- endif -%}
{%- assign display_type = product.type | replace: 'Glowy Body Moisturizer', '' | replace: 'Handmade Floral Soap', '' | replace: 'Handmade floral soap', '' | replace: 'Homemade Floral Soap', '' | replace: 'homemade floral soap', '' | replace: 'Cinnamon Café Latte Soap Bar', '' | replace: 'Cinnamon Cafe Latte Soap Bar', '' | replace: 'Cinnamon Café Latte Soap', '' | replace: 'Cinnamon Cafe Latte Soap', '' -%}
<article class="product-card{% if is_coming_soon %} coming-soon-card{% endif %}{% if is_sold_out %} sold-out-card{% endif %}">
  <a class="product-visual product-photo" href="{{ product.url }}">
    {%- if product.featured_image -%}
      {{ product.featured_image | image_url: width: 900 | image_tag: loading: 'lazy', alt: product.featured_image.alt | escape }}
    {%- else -%}
      <img src="{{ fallback_image | asset_url }}" alt="{{ product.title | escape }}">
    {%- endif -%}
    {%- if is_coming_soon -%}<div class="coming-soon-overlay">Launching soon</div>{%- endif -%}
  </a>
  {%- if display_type != blank -%}<p class="scent">{{ display_type }}</p>{%- endif -%}
  <h3><a href="{{ product.url }}">{{ display_title }}</a></h3>
  <div class="product-footer"><strong>{{ product.price | money }}</strong></div>
  {%- if is_sold_out -%}
    <a class="add-button product-card-link sold-out-button" href="{{ product.url }}">Sold Out For Now</a>
  {%- elsif is_coming_soon == false -%}
    <a class="add-button product-card-link" href="{{ product.url }}">View product</a>
  {%- else -%}
    <div class="launching-soon-box">Lip balms launching soon.</div>
  {%- endif -%}
</article>
